export default class GenUtils {

    /*
    returns a dictionary by getting the @param key out of the iteratable;
     */
    //TODO: convert this to a generic template with proper types
    // public static dictify<T, K extends keyof T>(iteratable: Iterable<T>, key: K) {
    //     var retDict: {K: T} = {};
    //     for (let obj in iteratable) {
    //
    //         if (iteratable.hasOwnProperty(obj)) {
    //             retDict[obj[key]] = obj;
    //         }
    //     }
    //     return retDict;
    // }

}